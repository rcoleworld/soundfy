//
// This controller handles features which are media centric such as commenting, liking,
// etc.
//
import express from 'express';

export default class MediaControlled {
  public path = '/media';
  public router = express.Router();

  // constructor() {}
}
